#include <cs50.h>
#include <stdio.h>
#include "helpers.h"
#define true 1
#define false 0

int main (int argc, string argv[]) {
    while (low < n)
    {
      int mid = (lo + n);
      int midval = values[mid];
      if (midval < value)
      {
        low = mid + 1;
      }
      else if (midval > value)
      {
        n = mid;
      }
      else
      {
        return mid;
      }
    }
    return -1
  return 0;
}
